---
title: "Projet IF36 - Analyse des données de Kickstarter"
output:
  prettydoc::html_pretty:
    theme: hpstr
    highlight: github
---

L'objectif de ce projet d'IF36 est d'analyser et de présenter des données issues d'un jeu de données de notre choix.

## 👥 Membres

Équipe Civizualisation :

- Florian BONELLI
- Eyvan FAURE
- Ryan OUYED
- Lucas SAURON

## 📚 Sommaire

- 📝 Contexte des données choisis
- 📥 Téléchargement des données
- 📁 Présentation des données
- 📊 Plan d'analyse
- 🧭 Exploration des données

## 📝 Contexte des données choisis

[![Kickstarter](https://i.kickstarter.com/tq0sfld-kickstarter-logo-green.png?origin=static&sig=f4aHZ5ZiQhwZCgzBFzJqR3WIwCUT6jVw6mALfOHvmu0%3D)](https://www.kaggle.com/datasets/kemical/kickstarter-projects?select=ks-projects-201801.csv)

Nous avons choisi d'utiliser les données du site de financement participatif Kickstarter.

Kickstarter offre l'opportunité à des créateurs de transformer des idées en réalité grâce au financement participatif.

Cette plateforme permet de lever des fonds pour financer des projets en mettant en relation des contributeurs du monde entier.

Nous avons décidé d'utiliser le jeu de données [Kickstarter Projects](https://www.kaggle.com/datasets/kemical/kickstarter-projects) disponible sur Kaggle.

Ces données sont collectées depuis la plateforme [Kickstarter](https://www.kickstarter.com/).

Ce projet nous permettra d'explorer les tendances du financement participatif, d'identifier les facteurs qui influencent le succès d'un projet et d'évaluer la probabilité de réussite des campagnes sur cette plateforme.

Nous chercherons également à visualiser ces données de manière claire et intuitive pour mieux comprendre les dynamiques du financement participatif.

## 📥 Téléchargement des données

Les données utilisées dans ce projet ne sont pas incluses dans ce dépôt en raison de leur taille.

Il faut les télécharger manuellement.

Suivez les étapes ci-dessous pour récupérer les fichiers nécessaires :

1. Téléchargez l'archive contenant les jeux de données.

    ```bash
    curl -L -o ./data/kickstarter-projects.zip https://www.kaggle.com/api/v1/datasets/download/kemical/kickstarter-projects
    ```

2. Une fois l'archive téléchargée, extrayez les fichiers.

    ```bash
    unzip ./data/kickstarter-projects.zip -d ./data
    ```

    Si `unzip` n'est pas installé, vous pouvez l'installer avec la commande suivante

    ```bash
    sudo apt-get install unzip
    ```

3. (Facultatif) Après avoir extrait les fichiers, vous pouvez supprimer l'archive pour libérer de l'espace.

    ```bash
    rm ./data/kickstarter-projects.zip
    ```

## 📁 Présentation des données

### Premier fichier : `ks-projects-201612.csv`

Ce premier jeu de données au format CSV (Comma-Separated Values) contient 321 616 individus et 17 variables.

| Variable           | Type de données | Description                                                                            |
|--------------------|-----------------|----------------------------------------------------------------------------------------|
| `ID`               | Integer         | Identifiant unique du projet                                                           |
| `name`             | String          | Nom du projet                                                                          |
| `category`         | String          | Catégorie spécifique du projet                                                         |
| `main_category`    | String          | Catégorie principale du projet                                                         |
| `currency`         | String          | Devise utilisée pour le financement                                                    |
| `deadline`         | Date            | Date limite pour la collecte des fonds                                                 |
| `goal`             | Float           | Montant que le projet espère récolter                                                  |
| `launched`         | Date and time   | Date et heure de lancement du projet                                                   |
| `pledged`          | Float           | Somme totale engagée par les contributeurs                                             |
| `state`            | String          | État de la campagne (ex : "failed", "successful")                                      |
| `backers`          | Integer         | Nombre de contributeurs                                                                |
| `country`          | String          | Pays où le projet est lancé (ISO 3166)                                                 |
| `usd pledged`      | Float           | Montant collecté en USD (conversion réalisée par Kickstarter)                          |
| NA                 | NA              | NA                                                                                     |
| NA                 | NA              | NA                                                                                     |
| NA                 | NA              | NA                                                                                     |
| NA                 | NA              | NA                                                                                     |

On remarque que les 4 dernières variables sont sans titre et sans données.

### Deuxième fichier : `ks-projects-201801.csv`

Ce deuxième jeu de données au format CSV (Comma-Separated Values) contient 375 765 individus et 15 variables.

| Variable           | Type de données | Description                                                                            |
|------------------- |-----------------|----------------------------------------------------------------------------------------|
| `ID`               | Integer         | Identifiant unique du projet                                                           |
| `name`             | String          | Nom du projet                                                                          |
| `category`         | String          | Catégorie spécifique du projet                                                         |
| `main_category`    | String          | Catégorie principale du projet                                                         |
| `currency`         | String          | Devise utilisée pour le financement                                                    |
| `deadline`         | Date            | Date limite pour la collecte des fonds                                                 |
| `goal`             | Float           | Montant que le projet espère récolter                                                  |
| `launched`         | Date and time   | Date et heure de lancement du projet                                                   |
| `pledged`          | Float           | Somme totale engagée par les contributeurs                                             |
| `state`            | String          | État de la campagne (ex : "failed", "successful")                                      |
| `backers`          | Integer         | Nombre de contributeurs                                                                |
| `country`          | String          | Pays où le projet est lancé (ISO 3166)                                                 |
| `usd pledged`      | Float           | Montant collecté en USD (conversion réalisée par Kickstarter)                          |
| `usd_pledged_real` | Float           | Montant collecté en USD (conversion réalisée par l'API fixer.io)                       |
| `usd_goal_real`    | Float           | Montant que le projet espère collecter en USD (conversion réalisée par l'API fixer.io) |

Ce fichier contient plus d'individus que le premier, exclut les variables inutiles et ajoute deux variables supplémentaires par rapport au premier fichier.

En observant juste les noms des fichiers, il est raisonnable de supposer que le fichier `ks-projects-201612.csv` contient des données jusqu'en décembre 2016, tandis que le fichier `ks-projects-201801.csv` inclut des données jusqu'en janvier 2018. Une vérification sera nécessaire pour s'assurer qu'il n'y a pas de doublons entre les individus présents dans ces deux fichiers.

De plus, si c'est le cas, le fichier de 2018 semble être plus pertinent à utiliser seul. En effet, il ne comporte pas les variables vides présentes dans le fichier de 2016, inclut deux variables supplémentaires (`usd_pledged_real` et `usd_goal_real`), contient un plus grand nombre d'individus et propose des données plus récentes.

## 📊 Plan d'analyse

### 🎯 Objectif principal de l'analyse

L'objectif principal de notre analyse est de comprendre les dynamiques du financement participatif sur Kickstarter. Plus précisément, nous cherchons à identifier des tendances utiles pour optimiser les chances de succès des projets afin de dégager les facteurs qui influencent la réussite ou l'échec d'une campagne.

### 🔍 Questions d'analyse

- Quels sont les taux de réussite des campagnes selon les catégories ?
- Dans quelle mesure le montant de l'objectif influence-t-il la probabilité de succès d'une campagne ?
- Existe-t-il des périodes de l'année où les campagnes ont plus de chances de réussir ?
- Quels sont les pays où le plus de campagnes sont créées? Parmi eux, lesquels ont les meilleurs taux de réussite?
- L'écart entre l'objectif initial et les fonds réellement collectés varie-t-il en fonction de la catégorie ou de la devise utilisée ?
- Le choix des mots dans le titre d'une campagne a-t-il un impact sur son succès ?
- Comment a évolué le taux de réussite et le nombre de campagne au fil des années? Les campagnes sont elles plus ambitieuses qu'avant?

### 📈 Variables à comparer

Pour répondre à nos questions d'analyse, nous comparerons les variables suivantes :

- **Taux de réussite et d'échec par catégorie** : Identifier les catégories où les campagnes réussissent ou échouent le plus souvent.
- **Taux de réussite et d'échec en fonction du montant de l'objectif** : Analyser l'impact du montant demandé sur le succès ou l'échec des campagnes.
- **Taux de réussite et d'échec selon la période de lancement** : Étudier si certaines périodes de l'année influencent les chances de succès ou d'échec.
- **Nombre de campagnes créées et leur taux de réussite selon le pays, sur une période donnée**: Comparer les pays où les campagnes sont les plus populaires pour observer des différences de nombres de campagnes et de taux de réussite selon les régions.
- **Écart entre le montant collecté et l'objectif fixé** : Comparer les fonds espérés et les fonds réellement obtenus pour évaluer les écarts.
- **Impact des mots-clés dans les titres sur le succès** : Identifier les mots les plus fréquents dans les titres des campagnes réussies et leur influence.
- **Nombre de campagnes réussies montant moyen récolté par campagne réussie selon l'année** : Comparer le nombre de campagnes réussies et leur valeur moyenne chaque année pour dégager des tendances.

Ces comparaisons nous permettront de mieux comprendre les facteurs influençant le succès des campagnes Kickstarter.

### ⚠️ Limitations et problèmes potentiels

- Certains porteurs de projets peuvent fixer des objectifs volontairement bas pour maximiser leurs chances de succès ou, au contraire, volontairement élevé par manque de sérieux, ce qui pourrait biaiser les résultats de l'analyse.

- Les taux de conversion en USD peuvent introduire des biais en fonction des fluctuations des taux de change et de l'inflation.

- Des facteurs externes, tels que les tendances économiques ou médiatiques, qui ne sont pas inclus dans les données, peuvent également influencer le succès des campagnes.

## 🧭 Exploration des données

Pour effectuer notre analyse, nous utilisons les libraries suivantes :

```{r echo=FALSE, warning=FALSE, cache=TRUE, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r message=FALSE, warning=FALSE}
library(ggplot2) # Pour la visualisation des données
library(dplyr) # Pour la manipulation des données
library(readr) # Pour la lecture des fichiers CSV
library(tidyr) # Pour le nettoyage et la transformation des données
library(scales) # Pour passer les échelles en nombres à virgule
library(stringr) # Pour l'analyse du texte
library(tidytext) # Pour l'analyse du texte
library(syuzhet) # Pour l'analyse émotionnelle du texte
library(GGally) # Pour les matrices de données
library(ggcorrplot) # Pour les matrices de corrélation
library(countrycode) # Pour les noms de pays entiers
library(maps) # Pour la carte du monde
library(lubridate) # Pour traiter les dates
library(ggrepel) # Pour l'espacement des légendes sur ggplot
library(gridExtra) # Pour afficher plusieurs graphiques côte à côte
library(treemapify) # Pour des graphiques en treemap
```

Nous chargeons les deux fichiers de données nécessaires à notre analyse :

```{r message=FALSE, warning=FALSE}
data2016 <- read_csv("./data/ks-projects-201612.csv", show_col_types = FALSE)
data2018 <- read_csv("./data/ks-projects-201801.csv", show_col_types = FALSE)
```

### Nettoyage des données

Assurons nous que nos données ne contiennent aucun individus doublon Pour cela nous allons utiliser la variable ID qui se veut unique pour chaque individu

Regardons tout d'abord si dans chaque fichier, un individu n'a pas de doublon dans le même fichier

Pour vérifier le nombre d'IDs uniques et les doublons dans les fichiers `ks-projects-201612.csv` et `ks-projects-201801.csv`, nous allons effectuer les étapes suivantes :

Nous comptons le nombre total d'IDs, le nombre d'IDs uniques, et calculons le nombre d'IDs en doublon en soustrayant les IDs uniques des IDs totaux.

```{r}
cat("Nombre d'IDs uniques dans ks-projects-201612.csv :", length(unique(data2016$ID)), "\n")
cat("Nombre d'IDs dans ks-projects-201612.csv :", length(data2016$ID), "\n")
cat("Nombre d'IDs doublon dans ks-projects-201612.csv :", length(data2016$ID) - length(unique(data2016$ID)), "\n")
```

```{r}
cat("Nombre d'IDs uniques dans ks-projects-201801.csv :", length(unique(data2018$ID)), "\n")
cat("Nombre d'IDs dans ks-projects-201801.csv :", length(data2018$ID), "\n")
cat("Nombre d'IDs doublon dans ks-projects-201801.csv :", length(data2018$ID) - length(unique(data2018$ID)), "\n")
```

Nous avons aucun doublons dans chacun de nos 2 fichiers.

Maintenant si on regarde nos 2 fichiers dans leur ensemble, regardons si nous avons des ids de 2016 qui sont dans 2018 et inversement.

```{r}
cat("IDs uniquement présents dans ks-projects-201612.csv :", length(setdiff(data2016$ID, data2018$ID)), "\n")
cat("IDs uniquement présents dans ks-projects-201801.csv :", length(setdiff(data2018$ID, data2016$ID)), "\n")
```

On peut observer que le nombre d'id dans data2016 qui ne se trouvent pas dans data2018 est égal à 0. Autrement dit, toutes les individus du fichier data2016 se trouvent dans data2018.

Pour conclure, nous pouvons donc utiliser le fichier `ks-projects-201801.csv` seulement sans utiliser le fichier ks-projects-201612.csv pour éviter les doublons et avoir plus de données.

```{r}
data_kickstarter <- data2018
```

#### Vérifions s'il y a des lignes entièrement vides

```{r}
cat(sum(rowSums(is.na(data_kickstarter)) == ncol(data_kickstarter)))
```

#### Nettoyage des individus comportant des valeurs NA

```{r}
print(sapply(data_kickstarter, function(x) sum(is.na(x))))

data_kickstarter <- data_kickstarter %>%
  drop_na()

print(sapply(data_kickstarter, function(x) sum(is.na(x))))
```

#### Nettoyage des individus comportant des valeurs non valides

```{r}
data_kickstarter <- data_kickstarter %>%
  filter(state != "undefined")

data_kickstarter <- data_kickstarter %>%
  filter(country != "N,0\"")
```

#### Conversion de la caractéristique "launched" en format Date

```{r}
data_kickstarter$launched <- as.Date(data_kickstarter$launched)
```

#### Nettoyage des individus dont la date de lancement est incohérente

```{r}
data_kickstarter <- data_kickstarter %>%
  filter(year(launched) >= 2009)
```

#### Conversion des codes pays en noms de pays anglais

```{r}
# Conversion des codes pays en noms de pays anglais
data_kickstarter <- data_kickstarter %>%
  mutate(country = countrycode(country, origin = "iso2c", destination = "country.name.en"))

# Modifications des noms de pays pour les rendre compatible avec la carte du monde
data_kickstarter <- data_kickstarter %>%
  mutate(
    country = case_when(
      country == "Hong Kong SAR China" ~ "Hong Kong",
      country == "United States" ~ "USA",
      TRUE ~ country
    )
  )
```

### Présentation du dataset

Notre dataset présente l'ensemble des projets lancés sur la plateforme Kickstarter depuis son ouverture en 2013.

On y trouve notamment les dates de lancement des projets, leur catégorie, leur pays d'origine, les montants demandés et récoltés par chaque projet et leur état d'avancement.

```{r}
glimpse(data_kickstarter)
```

Les projets Kickstarter sont de différentes natures, on y trouve majoritairement des projets culturels et artistiques mais également des projets technologiques ou de financement de petites entreprises. Voyons les catégories les plus populaires du dataset :

```{r}
# NOTE: DUPLICATA du graphique dans la question associée
campaign_counts <- data_kickstarter %>%
  group_by(main_category) %>%
  summarise(total_campaigns = n()) %>%
  arrange(desc(total_campaigns))

campaign_counts %>%
  ggplot(
    aes(
      x = reorder(main_category, total_campaigns),
      y = total_campaigns,
    )
  ) +
  geom_col(
    fill = "steelblue",
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Nombre total de campagnes Kickstarter par catégorie entre 2009 et 2018",
    x = "Catégorie",
    y = "Nombre total de campagnes",
  ) +
  theme_minimal() +
  coord_flip()
```

Tous les projets doivent définir un "Objectif" (`goal`), qui est la somme d'argent minimale pour que le projet soit considéré comme financé. Pendant la durée de la campagne, les utilisateurs de Kickstarter peuvent donner de l'argent au projet. La somme totale récoltée par le projet est visible dans la colonne `pledged` du dataset.

Cependant, ces valeurs monétaires sont données dans leur devise d'origine.

Pour que les comparaisons selon la devise aient plus de sens, les colonnes `usd_goal_real` et `usd_pledged_real` font la conversion des sommes en dollar étasunien (USD)

Voici les distributions des projets selon leur objectif `goal` et les sommes réellement récoltées `pledged`

```{r}
# Histogramme du montant cible (goal)
hist_goal <- data_kickstarter %>%
  ggplot(
    aes(
      x = usd_goal_real,
    )
  ) +
  geom_histogram(
    binwidth = 5000,
    fill = "steelblue",
    color = "white",
    boundary = 0,
    closed = "left",
  ) +
  scale_x_continuous(
    limits = c(0, 100000),
    breaks = seq(0, 100000, 20000),
    labels = label_comma(big.mark = " "),
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Montants cibles des campagnes Kickstarter",
    x = "Montant cible ('Goal') en USD",
    y = "Nombre de campagnes",
  ) +
  theme_minimal()

# Histogramme du montant récolté (pledged)
hist_pledged <- data_kickstarter %>%
  ggplot(
    aes(
      x = usd_pledged_real
    )
  ) +
  geom_histogram(
    binwidth = 5000,
    fill = "#228B22",
    color = "white",
    boundary = 0,
    closed = "left",
  ) +
  scale_x_continuous(
    limits = c(0, 100000),
    breaks = seq(0, 100000, 20000),
    labels = label_comma(big.mark = " "),
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Montants récoltés des campagnes Kickstarter",
    x = "Montant récolté ('Pledged') en USD",
    y = "Nombre de campagnes",
  ) +
  theme_minimal()

grid.arrange(hist_goal, hist_pledged, ncol = 2)
```

Chose étrange : Il y a davantage de projets avec des petits objectifs que de projets avec peu de fonds récoltés. Cela est peu réaliste, il faut voir s'il y a un problème avec le dataset.

Chaque financeur est appelé un `backer`. Le dataset associe à chaque projet la quantité de personnes qui l'ont financé :

```{r}
data_kickstarter %>%
  ggplot(aes(x = backers)) +
  geom_histogram(
    binwidth = 500,
    fill = "steelblue",
    color = "white",
    boundary = 0,
    closed = "left",
  ) +
  scale_x_continuous(
    limits = c(0, 10000),
    breaks = seq(0, 10000, 1000),
    labels = label_comma(big.mark = " "),
  ) +
  scale_y_continuous(
    limits = c(0, 1200),
    breaks = seq(0, 1200, 200),
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Distribution du nombre de backers par projet",
    x = "Nombre de backers",
    y = "Nombre de projets",
  ) +
  theme_minimal()
```

Chaque projet ne peut être financé que dans un temps imparti, situé entre le lancement de la campagne (`launched`) et sa clôture (`deadline`). Aucun financement supplémentaire n'est accepté au delà de la clôture de la campagne.

Dans les visualisations suivantes, nous allons voir la quantité de projets lancés chaque année et les durées des campagnes de notre dataset.

```{r}
data_kickstarter %>%
  ggplot(aes(x = year(launched))) +
  geom_bar(
    fill = "steelblue",
  ) +
  scale_x_continuous(
    limits = c(2009, 2017),
    breaks = seq(2009, 2017, 1),
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Nombre de projets lancés chaque année",
    x = "Année",
    y = "Nombre de projets",
  ) +
  theme_minimal()
```

```{r}
# Calcul et visualisation de la durée des campagnes avec filtrage des campagnes avec durée < 5000 jours
data_kickstarter <- data_kickstarter %>%
  mutate(
    duree_campagne = as.numeric(as.Date(deadline) - as.Date(launched))
  ) %>%
  filter(duree_campagne < 5000)

data_kickstarter %>%
  ggplot(
    aes(
      x = duree_campagne
    )
  ) +
  geom_histogram(
    binwidth = 1,
    fill = "steelblue",
  ) +
  scale_x_continuous(
    breaks = seq(0, 100, 5),
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Distribution de la durée des campagnes Kickstarter",
    x = "Durée (jours)",
    y = "Nombre de projets",
  ) +
  theme_minimal()
```

On remarque que l'immense majorité des campagnes durent 30 jours, soit 1 mois. La deuxième durée la plus populaire est 60 jours, ce qui correspond à 2 mois.

On voit également des pics sur tous les multiples de 5 jours.

### Analyses Exploratoires

On cherche à étudier les possibles corrélations entre le montant demandé, le montant récolté, le nombre de backers et le résultat de la campagne :

```{r}
data_kickstarter_corr_money <- data_kickstarter %>%
  select(usd_goal_real, usd_pledged_real, state, backers)

data_kickstarter_corr_money %>%
  ggpairs(
    aes(
      color = state,
    ),
    lower = list(continuous = "points", combo = "dot_no_facet"),
  ) +
  theme_minimal(base_size = 8) +
  theme(axis.text = element_text(size = 3))
```

On observe déjà un phénomène intéressant :

Les projets qui demandent un montant très élevé ont tendance à échouer ou être annulé, et n'atteignent jamais le montant demandé.

A contrario, les projets qui reçoivent un montant très élevé de financement ont demandé à la base une somme beaucoup plus raisonnable.

On observe ainsi une claire dichotomie dans les graphes du côté inférieur gauche, entre des projets qui reçoivent énormément de fonds avec un objectif à la base très raisonnable, et des projets demandant un montant très élevé recevant des sommes très raisonnables et donc insuffisantes.

A noter: Cette analyse ne prend en compte que les projets dont le *goal* ou le *pledged* est relativement élevé. Or, comme on le voit dans les graphes de la diagonale, l'immense majorité des projets ont un *goal* et un *pledged* peu élevés qui les rend quasi invisible dans ce graphe.

Refaisons la même analyse en retirant les montants très élevés.

```{r}
data_kickstarter_corr_small_money <- data_kickstarter_corr_money %>%
  filter(usd_goal_real < 10000 & usd_pledged_real < 10000)

data_kickstarter_corr_small_money %>%
  ggpairs(
    aes(
      color = state,
    ),
    lower = list(continuous = "points", combo = "dot_no_facet")
  ) +
  theme_minimal(base_size = 8) +
  theme(axis.text = element_text(size = 3))
```

Pas sûr pour celle-ci

On fait la même analyse avec les durées de campagne pour voir si la durée de campagne a un lien avec la réussite d'une campagne

```{r}
data_kickstarter <- data_kickstarter %>%
  mutate(
    duree_campagne_num =  as.numeric(duree_campagne)/86400
  )

data_kickstarter_corr_time <- data_kickstarter %>% select(duree_campagne_num,usd_pledged_real,backers,state)

# data_kickstarter_corr_time <- data_kickstarter_corr_time %>% filter(usd_pledged_real>500)

ggpairs(data_kickstarter_corr_time,columns = 1:3, aes(color = state),
  # upper = list(continuous = "density", combo = "ggally_box_no_facet", discrete = "count", na = "na"),
  lower = list(continuous = "points", combo = "dot_no_facet")) +
  theme_minimal() +
  theme(axis.text = element_text(size = 3))
```

### Quels sont les taux de réussite des campagnes en fonction des catégories ?

Nous allons analyser les taux de réussite des campagnes Kickstarter en fonction des catégories. Cette analyse nous permettra d'identifier les catégories qui réussissent le mieux et celles qui rencontrent plus de difficultés.

#### Nombre total de campagnes Kickstarter par catégorie

Dans un premier temps, nous allons commencer par regarder le nombre total de campagnes Kickstarter par catégorie. Cela nous permettra de voir si certaines catégories sont sur-représentées ou sous-représentées.

```{r}
campaign_counts <- data_kickstarter %>%
  group_by(main_category) %>%
  summarise(total_campaigns = n()) %>%
  arrange(desc(total_campaigns))

campaign_counts %>%
  ggplot(
    aes(
      x = reorder(main_category, total_campaigns),
      y = total_campaigns,
    )
  ) +
  geom_col(
    fill = "steelblue",
  ) +
  scale_y_continuous(
    breaks = seq(0, max(campaign_counts$total_campaigns), 10000),
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Nombre total de campagnes Kickstarter par catégorie entre 2009 et 2018",
    x = "Catégorie",
    y = "Nombre total de campagnes",
  ) +
  theme_minimal() +
  coord_flip()
```

Cette visualisation montre que certaines catégories, comme **Film & Video** et **Music**, ont un nombre très élevé de campagnes, tandis que d'autres, comme **Dance**, en ont beaucoup moins. On observe aussi un écart significatif entre **Film & Video** et **Dance**.

#### Taux de réussite des campagnes Kickstarter par catégorie

Examinons maintenant le taux de réussite pour chaque catégorie. Pour cela, nous allons regarder le pourcentage de projets réussis dans une catégorie.

```{r}
success_rates <- data_kickstarter %>%
  group_by(main_category) %>%
  summarise(
    total = n(),
    successful = sum(state == "successful"),
    success_rate = successful / total
  ) %>%
  arrange(desc(success_rate))

success_rates %>%
  ggplot(
    aes(
      x = reorder(main_category, success_rate),
      y = success_rate,
    )
  ) +
  geom_col(
    fill = "forestgreen",
  ) +
  scale_y_continuous(
    labels = percent_format(),
  ) +
  labs(
    title = "Taux de réussite des campagnes Kickstarter par catégorie entre 2009 et 2018",
    x = "Catégorie",
    y = "Taux de réussite",
  ) +
  theme_minimal() +
  coord_flip()
```

Dans cette visualisation, nous observons que les catégories **Dance**, **Theater** et **Comics** affichent les taux de réussite les plus élevés, avec notamment près de **60%** de réussite pour ces deux catégories. Cela peut s'expliquer par une communauté plus engagée ou des projets bien ciblés dans ces domaines.

En revanche, les catégories comme **Technology** et **Journalism** ont les taux de réussite les plus faibles, autour de **20%**. Cela peut être dû à des projets plus complexes avec des coûts beaucoup plus élevés.

Cela peut expliquer en partie les différences de taux de réussite : les catégories avec moins de projets peuvent avoir une communauté plus ciblée et engagée.

```{r}
success_rates %>%
  ggplot(
    aes(
      area = total,
      fill = success_rate,
      label = main_category,
    )
  ) +
  geom_treemap() +
  geom_treemap_text(
    place = "center",
    color = "white",
  ) +
  labs(
    title = "Taux de réussite des campagnes Kickstarter par catégorie entre 2009 et 2018",
  )
```

```{r}
categories_for_map <- data_kickstarter %>%
#  group_by(main_category) %>%
  group_by(main_category, category) %>%
  summarise(
    total = n(),
    successful = sum(state == "successful"),
    success_rate = successful / total
  ) %>%
  arrange(desc(success_rate))

categories_for_map %>%
  ggplot(
    aes(
      area = total,
      fill = success_rate,
      label = category,
      subgroup = main_category,
    )
  ) +
  geom_treemap() +
  geom_treemap_subgroup_border() +
  geom_treemap_subgroup_text(
    place = "center",
    color = "white",
    alpha = 0.5,
    fontface = "italic",
  ) +
  geom_treemap_text(
    place = "center",
    color = "white",
    reflow = TRUE,
  ) +
  labs(
    title = "Taux de réussite des campagnes Kickstarter par catégorie entre 2009 et 2018",
    fill = "Taux de réussite",
  ) +
  theme(legend.position = "bottom")
```

#### Répartition des montants demandés par catégorie

Enfin, analysons la répartition des montants demandés dans chaque catégorie possible de projets. Cela nous permettra de vérifier si certaines catégories ont des budgets aux des montants plus élevés ou plus faibles.

```{r}
data_kickstarter <- data_kickstarter %>%
  mutate(
    goal_category = case_when(
      goal < 1000 ~ "< 1k",
      goal < 10000 ~ "1k - 10k",
      goal < 50000 ~ "10k - 50k",
      goal < 100000 ~ "50k - 100k",
      TRUE ~ "> 100k"
    )
  )

normalized_data <- data_kickstarter %>%
  group_by(main_category) %>%
  mutate(total_projects = n()) %>%
  ungroup() %>%
  group_by(main_category, goal_category) %>%
  summarise(
    project_count = n(),
    total_projects = first(total_projects),
    normalized_count = (project_count / total_projects),
    .groups = "drop"
  ) %>%
  ungroup()

normalized_data$goal_category <- factor(normalized_data$goal_category, levels = c("< 1k", "1k - 10k", "10k - 50k", "50k - 100k", "> 100k"))

normalized_data %>%
  ggplot(
    aes(
      x = reorder(main_category, total_projects),
      y = normalized_count, fill = goal_category
    ),
  ) +
  geom_bar(
    stat = "identity",
    position = position_stack(reverse = TRUE),
  ) +
  scale_y_continuous(
    labels = percent_format(),
  ) +
  scale_fill_manual(
    values = c(
      "< 1k" = "forestgreen",
      "1k - 10k" = "green",
      "10k - 50k" = "gold",
      "50k - 100k" = "darkorange",
      "> 100k" = "red"
    )
  ) +
  labs(
    title = "Répartition des projets par catégorie et montant demandé entre 2009 et 2018",
    x = "Catégorie",
    y = "Répartition des projets",
    fill = "Montant demandé (USD)",
  ) +
  theme_minimal() +
  theme(
    legend.position = "bottom",
    text = element_text(size = 10),
  ) +
  coord_flip()
```

Ce graphique révèle que certaines catégories, comme **Technology**, ont une proportion plus élevée de projets demandant des montants importants (2/3 des projets à plus de 10 000 dollars). A l'inverse la catégorie **Dance** regroupe des projets de plus petits montants (80% en dessous de 10 000 dollars).

Les projets nécessitant des financements plus importants, comme ceux de la catégorie **Technology**, ont souvent des taux de réussite plus faibles car ils impliquent des besoins financiers plus lourds. À l'inverse, des catégories comme **Dance** demandent des budgets plus modestes, ce qui permet à un plus grand nombre de projets de réussir sans exiger des sommes trop importantes.

Une exception notable est la catégorie **Journalism** qui, malgré des besoins financiers modestes, embarque peu de contributeurs, ce qui peut expliquer son faible taux de réussite.

### Combien de projets sont lancés par pays ? Quelle proportion de projets réussis ?

```{r}
data_kickstarter %>%
  ggplot(
    aes(
      x = reorder(country, table(country)[country]),
    )
  ) +
  geom_bar(
    fill = "forestgreen",
  ) +
  scale_y_log10(
    breaks = c(10, 100, 1000, 10000, 30000, 100000, 300000),
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Nombre de projets Kickstarter par pays",
    x = "Pays",
    y = "Nombre de projets",
    caption = "Note de lecture: l'axe des ordonnées est logarithmique",
  ) +
  coord_flip() +
  theme_minimal(base_size = 11)
```

```{r}
# Création d'un dataframe avec le nombre total de projets par pays
country_counts <- data_kickstarter %>%
  count(country, name = "projects") %>%
  as_tibble()
```

```{r}
country_counts %>%
  ggplot(
    aes(
      area = projects,
      fill = projects,
      label = country,
    )
  ) +
  geom_treemap(show.legend = FALSE) +
  geom_treemap_text(
    place = "center",
    color = "white",
    grow = TRUE,
  ) +
  labs(
    title = "Répartition des projets Kickstarter par pays",
    subtitle = "Surface proportionnelle au nombre de projets",
  )
```

```{r}
# Importation de la carte du monde
mapdata <- map_data("world") %>%
  rename(country = region)

# Fusion des données des projets avec la carte du monde
mapdata_projects <- left_join(mapdata, country_counts, by = "country")

# Génération de la visualisation de la carte du monde
mapdata_projects %>%
  ggplot(
    aes(
      x = long,
      y = lat,
      group = group,
    )
  ) +
  geom_polygon(
    aes(
      fill = projects,
    ),
    color = "grey60",
    linewidth = 0.2,
  ) +
  scale_fill_viridis_c(
    option = "viridis",
    na.value = "lightgrey",
    trans = "log10",
    breaks = c(1, 10, 100, 1000, 10000, 100000),
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Nombre de projets Kickstarter par pays",
    fill = "Nombre de projets",
    caption = "Les pays sans données sont affichés en gris clair",
  ) +
  theme_minimal() +
  theme(
    axis.title = element_blank(),
    axis.text = element_blank(),
    axis.ticks = element_blank(),
    panel.grid = element_blank(),
    legend.position = "bottom",
    legend.key.width = unit(2, "cm"),
  )
```

Ce graphique représente la quantité de projets lancés sur Kickstarter par pays.

On remarque une grande sur-représentation des États-Unis, pays de création de Kickstarter.

On remarque ensuite une proéminence des pays anglo-saxons dans les pays dont le plus de projets sont validés.

On peut lier cela au fait que Kickstarter a été lancé progressivement dans le monde. Pour avoir une vision qui reflète mieux la popularité de Kickstarter, on pourrait comparer les quantités de projets selon le temps dans un prochain livrable. On peut également expliquer cela par le fait que, dans de nombreux pays, une offre plus locale et adaptée a eu le temps de se développer, avant l'arrivée de Kickstarter ou en parallèle.

```{r}
# Calcul du cumul des projets par pays
data_kickstarter_by_country <- data_kickstarter %>%
  group_by(country) %>%
  arrange(launched) %>%
  mutate(cumulative_count = row_number())

# Sélection des 10 pays avec le plus de projets
top_ten_countries <- data_kickstarter %>%
  count(country, sort = TRUE) %>%
  slice_head(n = 10) %>%
  pull(country)

data_kickstarter_top_ten_countries <- data_kickstarter_by_country %>%
  filter(country %in% top_ten_countries)

# Extraction du point maximal pour chaque pays (pour annotation)
max_cumul <- data_kickstarter_top_ten_countries %>%
  group_by(country) %>%
  filter(cumulative_count == max(cumulative_count)) %>%
  ungroup()

# Visualisation de la croissance cumulée des projets par pays (Top 10)
ggplot(data_kickstarter_top_ten_countries, aes(
    x = launched,
    y = cumulative_count,
    color = country,
    group = country
  )) +
  geom_line() +
  geom_label_repel(
    data = max_cumul,
    aes(label = country),
    nudge_x = 150,
    direction = "y",
    hjust = "left",
    size = 2.5,
    min.segment.length = 0,
    max.overlaps = Inf,
    arrow = arrow(length = unit(0.015, "npc"))
  ) +
  scale_x_date(
    limits = c(as.Date("2010-01-01"), as.Date("2018-12-31")),
    date_breaks = "1 year",
    date_labels = "%Y"
  ) +
  scale_y_continuous(labels = label_comma(big.mark = " ")) +
  labs(
    title = "Croissance cumulée du nombre de projets Kickstarter (Top 10 pays)",
    x = "Date de lancement",
    y = "Nombre cumulé de projets"
  ) +
  theme_minimal() +
  theme(legend.position = "none")
```

On remarque que la quantité de projets des États-Unis et en partie dûe à une avance accumulée: De 2009 à 2013, Kickstarter était disponible uniquement aux États-unis.

Faisons un zoom sur les autres pays du Top 10 qui ne sont pas les États-Unis :

```{r}
# Exclure les États-Unis pour se concentrer sur les autres pays du Top 10
data_kickstarter_top_ten_countries_no_US <- data_kickstarter_top_ten_countries %>%
  filter(country != "USA")

# Calculer le point maximal pour chaque pays (pour annotation)
max_cumul_no_US <- data_kickstarter_top_ten_countries_no_US %>%
  group_by(country) %>%
  filter(cumulative_count == max(cumulative_count)) %>%
  ungroup()

# Visualisation de la croissance cumulée des projets par pays (hors USA)
data_kickstarter_top_ten_countries_no_US %>%
  ggplot(
    aes(
      x = launched,
      y = cumulative_count,
      color = country,
    )
  ) +
  geom_line(linewidth = 1) +
  geom_label_repel(
    data = max_cumul_no_US,
    aes(label = country),
    nudge_x = 150,
    direction = "y",
    hjust = "left",
    size = 2.5,
    min.segment.length = 0,
    max.overlaps = Inf,
    arrow = arrow(length = unit(0.015, "npc"))
  ) +
  xlim(as.Date("2013-01-01"), as.Date("2018-01-03")) +
  scale_x_date(
    date_breaks = "1 year",
    date_labels = "%Y",
    expand = expansion(mult = 0.05)
  ) +
  scale_y_continuous(
    labels = label_comma(big.mark = " "),
  ) +
  labs(
    title = "Croissance cumulée du nombre de projets Kickstarter (Top 10 pays hors USA)",
    x = "Date de lancement",
    y = "Nombre cumulé de projets"
  ) +
  theme_minimal() +
  theme(legend.position = "none")
```

On remarque que les pays où Kickstarter arrive plus tôt ont une croissance plus forte du nombre de projets lancés.

Cela peut s'expliquer par le fait que l'implantation rapide de son service permet de se faire une meilleure place dans le marché. Passé 2014, Kickstarter s'est installé dans des pays où une offre de financement participatif concurrente s'était déjà développé avant son arrivée, ce qui n'est pas le cas aux États-Unis où le concept était novateur en 2009.

Par exemple en France, Kickstarter est rarement cité comme la meilleure plateforme de crowdfunding: <https://bpifrance-creation.fr/encyclopedie/financements/financement-participatif/sites-plateformes-crowdfunding>

Des services plus adaptés se sont implantés avant son arrivée.

Dans la visualisation suivante, nous allons évaluer la répartition des projets selon leur statut dans les pays du top 10:

```{r}
data_kickstarter_top_ten_countries %>%
  ggplot(
    aes(
      x = reorder(country, -table(country)[country]),
      fill = state,
    )
  ) +
  geom_bar(
    position = "fill",
  ) +
  scale_fill_manual(
    breaks = c("suspended", "canceled", "failed", "live", "successful"),
    values = c("grey", "orange", "red", "blue", "green"),
  ) +
  scale_y_continuous(
    labels = percent_format(),
  ) +
  labs(
    title = "Répartition des statuts des projets selon le pays",
    x = "Code Pays",
    y = "",
    fill = "Statut",
  ) +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 30, size = 9))
```

On remarque que tous les pays ont des taux d'échec et de réussite relativement similaires.

On remarque une grande proportion de projets en cours au Japon. Cela s'explique par le fait que Kickstarter a été lancé en Septembre 2017 au Japon, soit un an avant la finalisation de notre base de donnée en 2018. Ainsi, il est attendu d'avoir une sur-représentation de projets en cours de financement: <https://www.theverge.com/2017/8/17/16161068/kickstarter-japan-launch-september>

```{r}
success_rates_by_country <- data_kickstarter %>%
  group_by(country) %>%
  summarise(
    total = n(),
    successful = sum(state == "successful"),
    success_rate = successful / total
  ) %>%
  arrange(desc(success_rate))

success_rates_by_country %>%
  ggplot(
    aes(
      x = reorder(country, success_rate),
      y = success_rate,
    )
  ) +
  geom_col(
    fill = "forestgreen"
  ) +
  scale_y_continuous(
    labels = percent_format()
  ) +
  labs(
    title = "Taux de réussite des campagnes Kickstarter par pays",
    x = "Pays",
    y = "Taux de réussite",
  ) +
  theme_minimal() +
  coord_flip()
```

```{r}
mapdata_success <- left_join(mapdata, success_rates_by_country, by = "country")

mapdata_success %>%
  ggplot(
    aes(
      x = long,
      y = lat,
      group = group,
    )
  ) +
  geom_polygon(
    aes(
      fill = success_rate,
    ),
    color = "grey60",
    linewidth = 0.2,
  ) +
  scale_fill_viridis_c(
    option = "viridis",
    na.value = "lightgrey",
  ) +
  labs(
    title = "Taux de réussite des campagnes Kickstarter par pays",
    fill = "Taux de réussite",
  ) +
  theme_minimal() +
  theme(
    axis.title = element_blank(),
    axis.text = element_blank(),
    axis.ticks = element_blank(),
    panel.grid = element_blank(),
    legend.position = "bottom",
    legend.key.width = unit(2, "cm"),
  )
```

On constate que les taux de réussite varient beaucoup selon le pays, de 37% à 15%. On constate que les deux pays avec le plus haut taux de réussite sont aussi ceux qui ont le plus de projets (mais ce n'est pas le cas au delà des deux premiers). Dans la suite de notre rapport, nous pourrions évaluer s'il existe une corrélation entre le nombre de projets lancés dans chaque pays et leur taux de réussite.

```{r}
success_rates_by_country_no_us <- success_rates_by_country %>%
  filter(country != "USA")

success_rates_by_country_no_us %>%
  ggplot(
    aes(
      x = total,
      y = success_rate,
      label = country,
    )
  ) +
  geom_point() +
  geom_label_repel(
    min.segment.length = 0,
    max.overlaps = Inf,
    size = 2,
  ) +
  theme_minimal()
```

Il n'y a pas de lien clair entre la quantité de projet et le taux de réussite, la majorité des pays avec des quantités de projets très similaires ont des taux de réussites très variés.

### Existe-t-il des périodes de l'année où les campagnes ont plus de chances de réussir ?

```{r}
# Ajout du mois en clair dans une nouvelle table
data_kickstarter2 <- data_kickstarter %>%
  mutate(mois = month(launched, label = TRUE, abbr = FALSE))

# Calcul du taux de réussite par mois
monthly_success_rate <- data_kickstarter2 %>%
  group_by(mois) %>%
  summarise(
    total = n(),
    success = sum(state == "successful"),
    success_rate = success / total,
    .groups = "drop"
  )

# Visualisation
ggplot(monthly_success_rate, aes(x = mois, y = success_rate)) +
  geom_col(fill = "forestgreen") +
  labs(
    title = "Taux de réussite mensuel des campagnes Kickstarter par date de lancement",
    x = "Mois",
    y = "Taux de réussite"
  ) +
  theme_minimal()
```

Ce graphique illustre le taux de réussite mensuel des campagnes Kickstarter selon la date de lancement. On observe que ce taux fluctue dans une plage relativement étroite, comprise entre 0,29 et 0,38. Bien que les taux de réussite semblent similaires, des tendances notables se dégagent, qui peuvent être expliquées par des facteurs contextuels.

En particulier, on remarque un **taux de réussite plus faible en décembre**, probablement en raison des périodes de fêtes. Pendant ce mois, de nombreuses personnes privilégient l'achat de cadeaux plutôt que de contribuer à des campagnes de financement participatif. Cela pourrait expliquer la baisse de l'engagement.

Par ailleurs, une tendance saisonnière apparaît durant l'été, période de vacances où les dépenses personnelles sont souvent consacrées à des loisirs, notamment les voyages. L'augmentation des coûts liés aux voyages pourrait réduire la capacité des individus à financer des projets, ce qui se traduit par une légère diminution du taux de réussite des campagnes lancées pendant cette période.

On peut se demander si la corrélation se poursuit au niveau de la cloture des campagnes ?

```{r}
# Ajout du mois en clair dans une nouvelle table
data_kickstarter2 <- data_kickstarter %>%
  mutate(mois = month(deadline, label = TRUE, abbr = FALSE))

# Calcul du taux de réussite par mois
monthly_success_rate <- data_kickstarter2 %>%
  group_by(mois) %>%
  summarise(
    total = n(),
    success = sum(state == "successful"),
    success_rate = success / total,
    .groups = "drop"
  )

# Visualisation
ggplot(monthly_success_rate, aes(x = mois, y = success_rate)) +
  geom_col(fill = "red") +
  labs(
    title = "Taux de réussite mensuel des campagnes Kickstarter",
    x = "Mois",
    y = "Taux de réussite"
  ) +
  theme_minimal()
```

Ce graphique illustre le taux de réussite mensuel des campagnes Kickstarter en fonction de leur date de clôture. On observe que les taux sont similaires à ceux basés sur la date de lancement, mais décalés d'un mois.

Pour vérifier si cela est possible, il faut qu'on observe la durée des campagnes ?

```{r}
# Calcul de la durée de la campagne et filtrage des campagnes avec durée < 5000 jours
data_kickstarter <- data_kickstarter %>%
  mutate(
    duree_campagne = as.Date(deadline) - as.Date(launched)
  ) %>%
  filter(duree_campagne < 5000)

# Calcul de la moyenne de la durée des campagnes
moyenne_duree <- mean(data_kickstarter$duree_campagne, na.rm = TRUE)

# Visualisation
ggplot(data_kickstarter, aes(x = launched, y = duree_campagne)) +
  geom_point(color = "blue", alpha = 0.5) + # Nuage de points
  geom_hline(yintercept = moyenne_duree, linetype = "dashed", color = "red") + # Ligne de moyenne
  labs(
    title = "Durée des campagnes Kickstarter",
    x = "Date de lancement",
    y = "Durée de la campagne (jours)"
  ) +
  theme_minimal() +
  annotate("text", x = as.Date("2015-01-01"), y = moyenne_duree + 100, label = paste("Moyenne = ", round(moyenne_duree, 1)), color = "red", hjust = 0)
```

Ce graphique représente la durée des campagnes sur Kickstarter, exprimée en jours. On constate qu'en moyenne, une campagne dure 34 jours. Cette observation confirme l'hypothèse précédemment formulée : le décalage d'un mois observé entre les taux de réussite selon que l'on se base sur la date de lancement ou sur la date de clôture s'explique par la durée moyenne des campagnes. Ainsi, d'après nos analyses, les périodes les plus favorables au succès d'un projet se situent entre février et juin, ainsi qu'entre septembre et novembre.

### Quelle est la durée idéale d'une campagne?

Comme nous l'avons vu précédemment, la majorité des campagnes durent 30 jours. C'est la valeur par défaut de la durée dans l'interface de création de campagne, où il est aussi affirmé que les campagnes de moins de 30 jours ont plus de chances de réussir. Est-ce vrai?

```{r}
# Calcul de la durée de la campagne et filtrage des campagnes avec durée < 5000 jours
data_kickstarter <- data_kickstarter %>%
  mutate(
    duree_campagne = as.Date(deadline) - as.Date(launched)
  ) %>%
  filter(duree_campagne < 5000)

success_rates_by_length <- data_kickstarter %>%
  group_by(duree_campagne) %>%
  summarise(
    total = n(),
    successful = sum(state == "successful"),
    success_rate = successful / total
  ) %>%
  arrange(desc(success_rate))

success_rates_by_length %>% ggplot(aes(x=duree_campagne,y=success_rate))+
  geom_line()+
  geom_smooth()+
  scale_x_continuous(breaks = seq(0, 100, 5))+
  theme_minimal()
```

### Comment les montants demandés et récoltés ont-ils évolué au cours du temps?

#### Evolution brute des montants

Dans cette partie, nous nous intérresserons à l'évolution des montants demandés et récoltés par les campagnes kickstarter selon les époques.

Tout d'abord, nous allons représenter le montant moyen demandé selon l'année

```{r}
data_kickstarter_par_an <- data_kickstarter %>%
  mutate(launch_year=year(launched)) %>%
  group_by(launch_year) %>%
  summarise(mean_goal = mean(usd_goal_real),median_goal = median(usd_goal_real),mean_pledge = mean(usd_pledged_real), median_pledge = median(usd_pledged_real), na.rm = TRUE)

data_kickstarter_par_an <- data_kickstarter_par_an %>% gather("median_goal","median_pledge",key="type_stat",value="value")

#data_kickstarter_par_an <- data_kickstarter_par_an %>% gather("mean_goal","median_goal","mean_pledge","median_pledge",key="type_stat",value="value")

data_kickstarter_par_an %>% ggplot(aes(x=launch_year, y=value, color = type_stat))+
    geom_line(linewidth=1.3)+
    geom_point()+
    scale_x_continuous(limits = c(2009,2017),breaks = seq(2009,2017, 1))+
 theme_minimal()
```

On voit que l'objectif médian devient plus ambitieux, alors que les montants récoltés ont eux tendance à baisser depuis 2013.

#### Quels sont les pourcentages de complétion des campagnes réussies?

Nous allons étudier l'évolution des pourcentages de complétion des projets réussis

```{r}
data_kickstarter_reussis <- data_kickstarter %>%
  filter(state=="successful") %>%
  mutate(completion_percentage = usd_pledged_real/usd_goal_real,launch_year=as.character(year(launched)))



data_kickstarter_reussis %>% ggplot(aes(x=launch_year,y=completion_percentage)) +
  geom_boxplot()+
  scale_y_continuous(labels = percent_format(),limits = c(0,2))

data_kickstarter_reussis %>% ggplot(aes(x=launch_year,y=completion_percentage)) +
  geom_boxplot()+
  scale_y_continuous(labels = percent_format())
```

On remarque que l'immense majorité des projets réussis dépassent de peu leur objectif, et ce peu importe l'époque.

On remarque cependant qu'au fil des années de plus en plus de projets pulvérisent leur objectif, et dans des proportions de plus en plus grandes

### Le choix des mots dans le titre d'une campagne a-t-il un impact sur son succès ?

#### La longueur du titre influence-t-elle le taux de succès d'une campagne ?

```{r}
data_kickstarter <- data_kickstarter %>%
  mutate(title_length_words = nchar(name))

# Calculer la moyenne de la longueur des titres par état
mean_title_length <- data_kickstarter %>%
  group_by(state) %>%
  summarise(mean_length = mean(title_length_words, na.rm = TRUE))

# Tracer un graphique à barres
ggplot(mean_title_length, aes(x = state, y = mean_length, fill = state)) +
  geom_col() +
  labs(
    title = "Moyenne de la longueur des titres par succès/échec",
    x = "État du projet",
    y = "Longueur moyenne du titre (nombre de mots)"
  ) +
  theme_minimal()
```

D'après ce graphique nous pouvons observer qu'il semble y avoir une relation entre la taille du titre et le fait qu'il réussie ou pas. Les projet qui sont successful ont en moyenne plus de caractères qu'un projet qui échoue, cependant cette différence n'est pas significative

#### Certains mots dans le titre sont-ils plus fréquents dans les campagnes réussies que dans les campagnes échouées ?

```{r}
data_kickstarter_successful <- data_kickstarter %>%
  filter(state == "successful")

words <- data_kickstarter_successful %>%
  select(name) %>%
  unnest_tokens(word, name)

# Enlever les mots très courants ("stop words") si besoin
data("stop_words") # du package tidytext
words_clean <- words %>%
  anti_join(stop_words, by = "word")

# Compter les mots les plus fréquents
word_counts <- words_clean %>%
  count(word, sort = TRUE)
top_words <- word_counts %>%
  slice_max(n, n = 20)

ggplot(top_words, aes(x = reorder(word, n), y = n)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(
    title = "Mots les plus fréquents dans les projets Kickstarter réussis",
    x = "Mot", y = "Fréquence"
  ) +
  theme_minimal()
```

Qu'en est il pour les campagnes non réussie ?

```{r}
data_kickstarter_failed <- data_kickstarter %>%
  filter(state == "failed")

words <- data_kickstarter_failed %>%
  select(name) %>%
  unnest_tokens(word, name)

# Enlever les mots très courants ("stop words") si besoin
data("stop_words") # du package tidytext
words_clean <- words %>%
  anti_join(stop_words, by = "word")

# Compter les mots les plus fréquents
word_counts <- words_clean %>%
  count(word, sort = TRUE)
top_words <- word_counts %>%
  slice_max(n, n = 20)

ggplot(top_words, aes(x = reorder(word, n), y = n)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(
    title = "Mots les plus fréquents dans les projets Kickstarter qui ont échoué",
    x = "Mot", y = "Fréquence"
  ) +
  theme_minimal()
```

On s'aperçoit rapidement que les mots les plus utilisés n'ont pas de lien avec la réussite ou non d'un projet mais plutôt avec les catégories les plus représentés, biaisant ainsi l'analyse des mots fréquents dans les titres. Pour corriger ce biais, nous allons normaliser la fréquence des mots en tenant compte de la proportion de chaque catégorie, en multipliant la fréquence brute d'un mot par l'inverse de la proportion de sa catégorie.

```{r}
words <- data_kickstarter_successful %>%
  select(name, main_category) %>%
  unnest_tokens(word, name)

# Enlever les mots très courants ("stop words") si besoin
data("stop_words") # du package tidytext
words_clean <- words %>%
  anti_join(stop_words, by = "word")

# Calculer la proportion de chaque catégorie
category_proportions <- data_kickstarter %>%
  group_by(main_category) %>%
  summarise(category_count = n()) %>%
  mutate(category_proportion = category_count / sum(category_count))

# Compter les mots les plus fréquents par catégorie
word_counts <- words_clean %>%
  group_by(word, main_category) %>%
  count() %>%
  left_join(category_proportions, by = "main_category")

# Normaliser la fréquence des mots par l'inverse de la proportion de la catégorie
word_counts <- word_counts %>%
  mutate(normalized_frequency = (n / category_proportion))
top_words <- word_counts %>%
  arrange(desc(normalized_frequency)) %>%
  slice_head(n = 20)

top_words <- word_counts %>%
  ungroup() %>%
  arrange(desc(normalized_frequency)) %>%
  slice_head(n = 20)

ggplot(top_words, aes(x = reorder(word, normalized_frequency), y = normalized_frequency)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(
    title = "Mots les plus fréquents dans les projets Kickstarter échoués (fréquence normalisée)",
    x = "Mot", y = "Fréquence normalisée"
  ) +
  theme_minimal()
```

```{r}
words <- data_kickstarter_failed %>%
  select(name, main_category) %>%
  unnest_tokens(word, name)

# Enlever les mots très courants ("stop words") si besoin
data("stop_words") # du package tidytext
words_clean <- words %>%
  anti_join(stop_words, by = "word")

# Calculer la proportion de chaque catégorie
category_proportions <- data_kickstarter %>%
  group_by(main_category) %>%
  summarise(category_count = n()) %>%
  mutate(category_proportion = category_count / sum(category_count))

# Compter les mots les plus fréquents par catégorie
word_counts <- words_clean %>%
  group_by(word, main_category) %>%
  count() %>%
  left_join(category_proportions, by = "main_category")

# Normaliser la fréquence des mots par l'inverse de la proportion de la catégorie
word_counts <- word_counts %>%
  mutate(normalized_frequency = (n / category_proportion))
top_words <- word_counts %>%
  arrange(desc(normalized_frequency)) %>%
  slice_head(n = 20)

top_words <- word_counts %>%
  ungroup() %>%
  arrange(desc(normalized_frequency)) %>%
  slice_head(n = 20)

ggplot(top_words, aes(x = reorder(word, normalized_frequency), y = normalized_frequency)) +
  geom_col(fill = "steelblue") +
  coord_flip() +
  labs(
    title = "Mots les plus fréquents dans les projets Kickstarter échoués (fréquence normalisée)",
    x = "Mot", y = "Fréquence normalisée"
  ) +
  theme_minimal()
```

On voit que malgré après avoir contrebalancé l'effet de la sur-représentations de certaines catégories visible à travers le titre ne permet pas de trouver des mots particuliers présent dans le titre des projets qui réussissent ou échouent. En dehors des mots qui se trouvent dans les catégories les plus représenté ce graphique nous confirme que les catégories tel que le journalisme et le design ont une proportion de projet qui échouent élevé. Si une analyse simple des mots présent dans le titre ne montre rien peut être qu'une analyse plus poussé basé sur des mots dit expressif ou à connotation positive ou négative peut avoir un impact.

#### L'utilisation de mots émotionnels ou marketing dans le titre est-elle corrélée avec un meilleur taux de succès ?

```{r}
# Calculer le score de sentiment pour chaque titre
data_kickstarter_sentiment <- data_kickstarter %>%
  filter(state == "successful" | state == "failed") %>%
  mutate(
    sentiment_score = get_sentiment(name, method = "syuzhet")
  )

# Créer un graphique pour voir si le score de sentiment est corrélé au succès
ggplot(data_kickstarter_sentiment, aes(x = sentiment_score, fill = state)) +
  geom_histogram(position = "dodge", binwidth = 0.1, alpha = 0.7) +
  labs(
    title = "Corrélation entre le sentiment du titre et le succès du projet",
    x = "Score de sentiment (méthode Syuzhet)",
    y = "Fréquence"
  ) +
  theme_minimal() +
  scale_fill_manual(values = c("red", "green"), labels = c("Échoué", "Réussi"))
```

Ce graphique illustre la corrélation entre le sentiment des titres des projets Kickstarter (mesuré par la méthode Syuzhet, de -4 à 4) et leur succès, distinguant les projets "Échoués" (rouge) et "Réussis" (vert). On observe que la plupart des projets, qu'ils aient réussi ou échoué, ont un score de sentiment proche de 0 (neutre), avec un pic marqué à cette valeur. Les projets échoués prédominent légèrement dans les scores négatifs (\<-2), tandis que les projets réussis sont un peu plus fréquents dans les scores positifs (\>2). Cette analyse suggère que le sentiment du titre n'influence que marginalement le succès d'un projet, confirmant que d'autres facteurs pourraient jouer un rôle plus déterminant dans les résultats des campagnes Kickstarter.
